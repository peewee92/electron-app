import { useNavigate } from 'react-router-dom'
import './home.less'

function Home() {
    // 创建路由钩子
    const navigate = useNavigate()

    return (
        <div>home</div>
    )
}


export default Home